# Workline Bug List & Resolution Register

| BUG-ID | Severity | Subsystem | Description | Reproduction | Root Cause | Fix | Regression Test | Status |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| **BUG-001** | HIGH | Frontend / Next.js Turbopack | Vercel production build failure on Next.js 16 during static page generation. | `next build` on Next.js 16 without Clerk authentication keys. | Unhandled error in Next.js worker prerender evaluation when `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` is missing in build environment. | Added fallback key in `RootLayout` and safe error boundary in `middleware.ts`. | `npm run build` | **RESOLVED** |
| **BUG-002** | MEDIUM | Frontend / Workspaces | npm warnings emitted regarding `allowScripts` in workspace packages. | `npm install` on npm 10+ with workspace-level `allowScripts`. | npm workspaces ignore workspace-level `allowScripts` and require them at the root `package.json`. | Moved `allowScripts` exclusively to root `package.json`. | `npm run typecheck` | **RESOLVED** |
| **BUG-003** | HIGH | Frontend / Auth Components | Module export error during Next.js 16 client SSR evaluation: `Export SignedIn doesn't exist in target module`. | `next build` evaluating `page.tsx` importing `<Show>` / `<SignedIn>`. | In `@clerk/nextjs` 7.x, named JSX wrapper exports trigger Turbopack module resolution errors. | Refactored auth state rendering to standard `useAuth()` hook (`isSignedIn`). | `npm run typecheck` & `npm run build` | **RESOLVED** |
| **BUG-004** | LOW | Frontend / Configuration | Deprecation warning for `eslint` block inside `next.config.ts`. | `next build` on Next.js 16. | Next.js 16 deprecated `eslint` options in `next.config.ts`. | Cleaned obsolete `eslint` key from `frontend/next.config.ts`. | `npm run build` | **RESOLVED** |
