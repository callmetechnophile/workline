"""Domain module commands removed — these capabilities live inside WORKLINE.

Run `wg open` to access requirements, architecture, components, BOM, research,
tasks, decisions, documents, analysis, agents, and team inside WORKLINE.
"""
raise ImportError(
    "wg domain commands (requirements, components, bom, etc.) have been removed. "
    "Use `wg open` to access these capabilities inside WORKLINE."
)
